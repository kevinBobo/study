server:
  port: 8085

spring:
  application:
    name: sleuth-server

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/
management:
  metrics:
    web:
      server:
        auto-time-requests: false
